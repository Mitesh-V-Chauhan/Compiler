fn add(a: *int, b: *int) -> int {
    return *a + *b;
}

fn main() -> int {
    let x: int = 20;
    let y: int = 30;
    
    let ptr_x: *int = &x;
    let ptr_y: *int = &y;
    
    *ptr_x = 50;
    
    return add(ptr_x, ptr_y);
}
